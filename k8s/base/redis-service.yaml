apiVersion: v1
kind: Service
metadata:
  name: biddinggo-redis
  namespace: biddinggo
  labels:
    app: biddinggo-redis
spec:
  type: ClusterIP
  selector:
    app: biddinggo-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
