apiVersion: v1
kind: Service
metadata:
  name: biddinggo-backend-service
  namespace: biddinggo
  labels:
    app: biddinggo-backend
spec:
  type: ClusterIP
  selector:
    app: biddinggo-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
